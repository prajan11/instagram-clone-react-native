import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
    },
    name:{
        alignSelf: 'center',
        color: '#4f4f4f',
        fontWeight: 'bold',
    }
});

export default styles;