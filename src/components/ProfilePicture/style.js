import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container:{
        borderColor: '#d742f5',
        borderWidth: 3,
        borderRadius: 40,
        margin: 6,
    },
    image: {
        borderRadius: 40,
        borderColor: '#ffffff',
        borderWidth: 1,
        
    }
});

export default styles;